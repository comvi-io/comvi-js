<!-- Report-only — the script block is never closed, so extraction and the
     position remap are impossible. -->
<script setup lang="ts">
import { useI18n } from "@comvi/vue";
const { reloadTranslations } = useI18n();
