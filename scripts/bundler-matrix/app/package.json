{
  "name": "bundler-matrix-app",
  "private": true,
  "type": "module",
  "description": "Standalone consumer fixture for the pack-based bundler-matrix gate. NOT part of the pnpm workspace: installed with npm at runtime so the packed @comvi tarballs resolve exactly like a published install (run.mjs adds the tarballs with `npm install --no-save`).",
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "react": "^19.0.0",
    "solid-js": "^1.9.12",
    "svelte": "^5.55.1",
    "vite": "^8.0.3",
    "vue": "^3.5.13",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
