node_modules
dist
dist-*
coverage
.turbo
.next
.nuxt
.output
.svelte-kit
playwright-report
test-results
packages/benchmark/results
pnpm-lock.yaml
.omx
scripts/bundler-matrix/app/out

# Frozen codemod inputs/outputs — the goldens assert exact text, and one of
# them is a deliberately unterminated <script> block (the extraction-failure
# report shape), which prettier cannot parse.
scripts/codemods/*/__fixtures__
