---
"@comvi/nuxt": patch
---

Bump `nuxt` and `@nuxt/schema` devDependencies from `^3.21.0` to `^3.21.4` to pull in patched transitive `defu@6.1.7` (security fix). No runtime API change for `@comvi/nuxt` consumers.
