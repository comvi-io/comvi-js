# Gate-E adversarial smoke build. Points the extension at a LOCAL mock API so
# the full session/proxy flow is exercisable without a real key or the
# production host. Never ship a build made with this env.
VITE_COMVI_API_BASE_URL=http://127.0.0.1:8791
