/**
 * Type definitions index
 *
 * Re-exports translation types from submodule.
 * API and system types are in the root types.ts file.
 */

// Re-export translation types (NodeData, ElementData, KeyInfo)
export * from "./translation";

// Note: TranslationSystemOptions, Language, and other API types
// are defined in ../types.ts to avoid duplication
