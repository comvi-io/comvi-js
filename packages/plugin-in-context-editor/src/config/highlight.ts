/**
 * Highlight configuration constants
 * Used by ElementHighlighter for visual overlay styling
 */

/**
 * Debounce delay for scroll events in milliseconds
 * Used to avoid excessive position updates during scrolling
 */
export const SCROLL_DEBOUNCE_DELAY = 100;

/**
 * Default z-index for highlight overlays
 * Ensures highlights appear above most page content
 */
export const DEFAULT_HIGHLIGHT_Z_INDEX = 10000;

/**
 * Default border width for highlight overlays in pixels
 */
export const DEFAULT_HIGHLIGHT_BORDER_WIDTH = 2;

/**
 * Default highlight border color — Comvi brand amber
 */
export const DEFAULT_HIGHLIGHT_BORDER_COLOR = "#D97706";

/**
 * Default highlight background color — semi-transparent brand amber
 */
export const DEFAULT_HIGHLIGHT_BACKGROUND_COLOR = "rgba(217, 119, 6, 0.12)";
